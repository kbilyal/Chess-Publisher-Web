# Desktop directional sync repair trigger

Apply and validate the explicit Pull Cloud → Desktop / Push Desktop → Cloud contract and align stale Web production gates.

Retry after aligning the legacy Hub Companion contract marker with directional fingerprint schema v6.
