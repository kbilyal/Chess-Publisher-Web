# Conflict resolution and federation deployment marker

Deploy implementation commit `087bbfbf8e740fbf27c3bc0918adec851328db89`.

This production update makes same-field Desktop/Cloud vs Web conflicts explicitly resolvable without silently overwriting either side, preserves non-overlapping changes, expands the federation catalogue, and renders a real selected federation flag image in the Web Companion (for example GRE — Greece).
