# Chess-Publisher Web — Handoff Entrypoint

A new developer or AI agent should begin here:

1. `AGENTS.md`
2. `PROJECT-STATE.md`
3. `INTEGRATION-GATE.md`
4. `NEXT-TASK.md`

Do not assume the project is fully integrated until `PROJECT-STATE.md` says `HANDOFF_READY = true` and every gate in `INTEGRATION-GATE.md` is PASS.

The repository, tests, and pinned upstream sources are the durable source of truth; historical chat context is optional.
