# Optional Gemini API key. Configure locally or in the production deployment
# only if a Chess-Publisher feature explicitly requires Gemini API access.
GEMINI_API_KEY=""

# Public application URL used for self-referential links and callbacks.
# Example: https://web.chess-publisher.org
APP_URL="http://localhost:3000"
